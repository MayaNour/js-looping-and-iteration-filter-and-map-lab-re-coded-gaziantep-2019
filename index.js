// Code your solution here:
function driversWithRevenueOver(drivers, revenueValue){
  const newDriversFilterList = drivers.filter({
    function (driver.name) { return driver.revenue > revenueValue; }
  });
  
  return newDriversFilterList;
}